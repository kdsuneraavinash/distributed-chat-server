package server.components.raft;

import server.components.ServerComponent;

public class RaftComponent extends ServerComponent {
    public RaftComponent(int port) {
        super(port);
    }

    @Override
    public void run() {
    }
}
