package server.components.gossip;

import server.components.ServerComponent;

public class GossipComponent extends ServerComponent {
    public GossipComponent(int port) {
        super(port);
    }

    @Override
    public void run() {
    }
}
